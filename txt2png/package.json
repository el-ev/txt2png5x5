{
	"name": "txt2png",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build-wasm": "wasm-pack build -t web && mkdir -p src/pkg public/pkg && cp -r ../pkg/* src/pkg && cp -r ../pkg/* public/pkg",
		"build": "npm run build-wasm && tsc",
		"deploy": "wrangler deploy",
		"start": "wrangler dev"
	},
	"devDependencies": {
		"typescript": "^5.9.2",
		"wrangler": "^4.33.2"
	}
}